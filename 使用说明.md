# CAN SI 仿真使用说明（TCAN1042 · PSpice 17.4）

> 适用范围：机械臂 FDCAN 链路物理层仿真（板卡 + 电机1 + 电机2，分叉拓扑，5Mbps）。
> 环境：OrCAD PSpice 17.4（Capture 自带）。

## 一、文件清单

| 文件 | 作用 |
|------|------|
| `can_si_2motor_transceiver.cir` | **正式仿真**（真实 TCAN1042 收发器模型 + 走线） |
| `can_si_2motor.cir` | 理想版（简化理解拓扑用，一般不用） |
| `models/TCAN1042_q1_pspice.lib` | TCAN1042 收发器明文模型库 |
| `can_si_2motor_transceiver.out` | 上次运行的控制台输出（含 `.PRINT` 数据表） |
| `can_si_2motor_transceiver.dat` | 上次运行的波形数据（Probe 打开用） |

## 二、打开并查看已有波形（最快）

1. 开始菜单打开 **PSpice AD**；
2. `File → Open Simulation Results` → 选 `can_si_2motor_transceiver.dat`；
3. `Trace → Add Trace`，添加以下表达式（可从列表点选）：

   - 板端差分：`V(CANH_B)-V(CANL_B)`
   - 电机1 桩端差分：`V(CANH_M1)-V(CANL_M1)`
   - 电机2 远端差分：`V(CANH_M2)-V(CANL_M2)`
   - RXD 解码：`V(RXD1)`、`V(RXD2)`、`V(RXD3)`
   - TXD 激励：`V(D_TXD1)`

4. 想看两栏对比：`Plot → Add Plot` 把差分和 RXD 分窗口。

## 三、重新运行仿真

1. PSpice AD：`File → Open` 选 `can_si_2motor_transceiver.cir`；
2. `Simulation → Run`（或 F11）；
3. 跑完自动进 Probe，按上面步骤加 Trace。

> 也可以用命令行无头跑（更省事，结果写入 `.out/.dat`）：
> `D:\Cadence\SPB_17.4\tools\bin\psp_cmd.exe can_si_2motor_transceiver.cir`
> 注意：**工作目录必须是 si_models**（模型 `.lib` 按 .cir 所在目录解析）。

## 四、改成你的实际接线（关键）

打开 `can_si_2motor_transceiver.cir`，改这几行（约 52~55 行）：

```spice
.PARAM L_TRUNK=1.0   ; 板卡 → 分叉点长度（米），填实际值
.PARAM L_SEG2 =1.5   ; 分叉点 → 电机2 长度（米），填实际值
.PARAM S_STUB =0.3   ; 分叉点 → 电机1 分叉长度（米），填实际值
.PARAM TDPM   =4.27n ; 线速延迟 = 1/(0.78×光速)，一般不用动
```

终端电阻（约 63~67 行 `RTH_A/RTL_A/RTH_M2/RTL_M2 = 60`）：

- **电机2 那端实际接了终端** → 保持 `60`；
- **实际没接终端** → 改成 `1Meg`（模拟开路），这常常就是"现场偶发掉线"的原因；
- 若实际是 CANH-CANL 之间一只 120Ω → 保持 `60`（等效相同）。

激励位率（`VTX1` 的 PWL 时间戳）：数据段不是 5Mbps 时，把所有时间戳按比例改
（如 2Mbps 就全 ×2，bit 从 200ns 变 500ns）。

## 五、怎么判断结果"合不合理可行"

跑完只看 3 条：

| 检查项 | 判据 | 当前仿真结果 |
|--------|------|--------------|
| 显性位差分 | ≥1.5V | ≈2.26V ✅ |
| 隐性位差分 | 回到 0V 附近 | ≈0V ✅ |
| RXD1/2/3 | 波形与 TXD 顺序一致 | 完全还原 ✅ |

三条都满足 = 这条接线物理层可行。

其他参考（当前仿真数据）：
- 单隐性放电：板端 5ns 释放，桩端/远端 15~25ns 回到隐性，下冲约 -0.18V；
- 上升沿远端过冲 ≈2.7V（约 18%），25ns 稳定；
- **桩长建议 ≤0.5m**（桩越长相位反射越深，1m 附近最差，实测 3m 桩下冲约 -0.96V，RXD 只判正阈值，仍不误触发但裕量下降）。

## 六、常见问题

- **报 `ORPSIM-16306`（节点名前缀）**：别把新节点名起成 A/B/J/K/M/N/P/Q/T/U/X/Z/`_` 开头；用 C/D/E/F/G/H/I/L/O/R/S/V/W/Y 开头最安全。
- **报 `ORPSIM-15090`（T 线 .PRINT 全挂）**：这是 17.4 的 T 元素 bug，别改电路——不要对 T 线附近节点用 `.PRINT`，改用 Probe 看 `.dat`。
- **改完别用中文写注释**：17.4 按 ANSI 读 .cir，中文注释会报错，全部用英文/数字。
- **想加桩长扫描**：文件末尾有注释掉的 `.STEP PARAM S_STUB LIST 0.25 0.5 1 2 3`，取消注释、把单跑值改回即可。**注意 S_STUB 不能为 0**（零长 T 线收敛失败）。
- **想验证"电机1 发送"**：文件文末有 Alt A 注释，把 `VTX2` 激励打开、`VTX1` 改空闲即可。