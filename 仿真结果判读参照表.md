# CAN FD 仿真结果判读参照表（7 关节 + 控制器）

> 仿真文件：`can_si_7joint.cir`（正式版，TCAN1042 真实模型）
> 判读对象：5 Mbps 数据段位序列 `1101000110`（含最恶劣的"长显性→单隐性"放电工况）
> 本表"当前实测"列 = 默认线长（主干每段 0.5m、关节桩 0.3m、总干线 2.5m）跑出的数值

## 一、看波形：打开 `.dat` 后加这几条 Trace

| 观测点 | Probe 表达式 | 看什么 |
|---|---|---|
| 控制器差分 | `V(RPR_INH)-V(RPR_INL)` | 源端总线电压 |
| 中间关节差分 | `V(RPR_J4H)-V(RPR_J4L)` | 链路中部（最接近反射叠加区） |
| 末端差分 | `V(RPR_J7H)-V(RPR_J7L)` | 终端端，最远端 |
| 某桩端差分 | `V(RPR_J1H)-V(RPR_J1L)` | 不终端桩，反射源 |
| 接收还原 | `V(RPR_RXD_IN)` … `V(RPR_RXD_J7)` | 8 个收发器 RXD 是否还原 TXD |
| 判据线（手动） | `Trace → Evaluate` 里加常数值 | 0.9V（接收阈值）/ 1.5V（合格线） |

## 二、核心判据表（逐项对照）

| # | 检查项 | 怎么读 | 合格判据 | 当前实测（默认线长） | 不合格含义 | 改什么 |
|---|---|---|---|---|---|---|
| 1 | 显性电平 | 各节点差分峰值 | **≥ 1.5V** | IN 2.46V / J1 2.79V / J4 2.90V / J7 2.71V | 显性打不满，收端可能判错 | 查终端电阻是否缺失；缩短总线总长；降数据段速率 |
| 2 | 隐性残留 | 单隐性位期间差分最小值 | 回落到 **0V 附近（\|x\|≤0.5V）** | IN -0.12V / J1 -0.28V / J4 -0.16V / J7 -0.04V | 残留大 = 总线过长/桩太多，放电慢 | 缩短主干/桩；减少分叉级数；两端各 120Ω 终端必须有 |
| 3 | 负向过冲（下冲） | 全波形差分最小值 | **≥ -0.5V** | IN -0.12V / J1 -0.28V / J4 -0.23V / J7 -0.14V | 桩端反射深，桩过长 | **缩短桩长 ≤0.5m**（分线板到关节的线） |
| 4 | 正向过冲 | 差分是否超过 3V | **≤ 3.0V（不触发器件应力）** | 全部 ≈2.7V 以下，无超 3V | 过冲大，收发器输入端过压 | 缩短桩；检查桩端是否有未终端分支 |
| 5 | 放电恢复时间 | 单隐性位 1415ns 后，差分回到 \|x\|<0.5V 的时间 | **≤ 25ns**（占 200ns 位的 12.5%） | J7 20ns，其余 <12ns | 恢复慢 → 紧邻的下一个显性位采样时电平不足 | 缩短总线；加终端 |
| 6 | 位采样正确性 | 每位中点 RXD 电平 | **RXD 与 TXD 位序一一对应**（0/1 全对） | 8 节点全部 `01010` ✓ | 任一节点错位 = 该节点真误码 | 先看它所在桩的差分是否在阈值附近振荡；缩短该桩或总线 |
| 7 | 位间裕量 | 采样点差分离 0.9V 阈值多远 | **显性≥1.5V、隐性≤0.5V，裕量 ≥1.0V** | 显性 2.46~2.90V（裕量 1.5V+）；隐性 0.04~0.28V（裕量 0.6V+） | 裕量小 = 危险，靠近误码边缘 | 优先缩短桩长；降速 |

## 三、"要不要改线长"决策树

```
① 先看 RXD 8 个节点是否都 01010
   ├─ 全部正确 → ②
   └─ 有 FAIL → 看该节点差分：显性不足？残留大？振荡？→ 缩短该桩 / 主干再跑
② 看 #1 显性 和 #4 过冲
   ├─ 显性 <1.5V 或 过冲 >3V → 缩短总长 / 检查终端
   └─ 合格 → ③
③ 看 #3 下冲
   ├─ 下冲 ≤ -0.5V（太深）→ 桩太长，把桩缩短到 0.3~0.5m 以内
   └─ 合格 → ④
④ 看 #5 恢复时间
   ├─ >25ns → 总线总长或桩总和过大，降速或缩短
   └─ 合格 → 这条接线在 5Mbps 下物理层可行 ✅
```

**经验阈值（默认线长已验证可行）**：
- 桩长（分线板→关节）每根 **≤ 0.5m**，越短越好（0.3m 时下冲 -0.28V，裕量充足）
- 主干（分线板之间）每段 ≤1m 一般没问题，但要控制**总线总长**（主干+桩总和）
- 总线总长经验上限 @5Mbps：**≤ 4~5m 较稳妥**；超了优先降数据段速率（2Mbps 位长翻倍，放电时间翻倍，能救回）

## 四、常见症状 → 原因 → 对策

| 症状 | 直接原因 | 对策 |
|---|---|---|
| 显性差分 <1.5V | 终端缺失/接触不良；总线太长 | 确认 IN 和 J7 两端各 120Ω；缩短总线 |
| 隐性残留 >0.5V | 总线过长放电慢；桩过多 | 缩短桩/主干；减少分线板级数；两端加终端 |
| 桩端差分剧烈振荡、下冲很深 | 桩太长（反射相位迟） | 桩缩短到 ≤0.5m（重点改 `L2/L4/L6/L7/L9/L10/L11`） |
| 个别关节 RXD 误码 | 该桩附近反射叠加 | 缩短该桩；或该桩端并一只小电容（工程对策） |
| 全部节点采样裕量都偏小 | 总线总长太长 | 降数据段速率（2Mbps）；或缩短总线 |

## 五、怎么改：线长参数位置（`can_si_7joint.cir` 中段）

```spice
.PARAM L1=0.5   ; IN  -> 分线板1
.PARAM L2=0.3   ; 分线板1 -> J1   (桩，重点改)
.PARAM L3=0.5   ; 分线板1 -> 分线板2
.PARAM L4=0.3   ; 分线板2 -> J2   (桩)
.PARAM L5=0.5   ; 分线板2 -> 分线板3
.PARAM L6=0.3   ; 分线板3 -> J3   (桩)
.PARAM L7=0.3   ; 分线板3 -> J4   (桩)
.PARAM L8=0.5   ; 分线板3 -> 分线板4
.PARAM L9=0.3   ; 分线板4 -> J5   (桩)
.PARAM L10=0.3  ; 分线板4 -> J6   (桩)
.PARAM L11=0.5  ; 分线板4 -> J7   (桩，终端端)
.PARAM TDPM=4.27n ; 线速延迟，勿动
```
改完重跑：`D:\Cadence\SPB_17.4\tools\bin\psp_cmd.exe can_si_7joint.cir`（工作目录 = si_models），然后看 `.out` 或 `.dat`。

## 六、模型注意事项（重要）

1. **模型已含 3 个 PSpice 移植补丁**（`.cir` 内覆盖 `.lib`，勿删）：`MUX2X1_0`（RXD 选通）、`LOGIC_IO_0`（RXD 输出级）、`BUF_0`（TXD/STB 输入缓冲）。
   - `BUF_0` 修复的是 TI TINA 模型在 PSpice 17.4 的动态缺陷：**长显性后接单隐性再接显性时，TXDint 会自己爬到 VDD 导致驱动提前释放（假误码）**。修复后已验证全序列正确。
2. 终端按"IN 与 J7 两端各 120Ω 跨差分"建模（2×60Ω 串联 + 中点 20k 锚定共模）。若实物是 60+60 到地，改法见文件注释。
3. 隐性期共模靠 20k 锚定（模型无隐性偏置），只看**差分**，别用单端电压判好坏。
4. PSpice 17.4 无 `.MEAS`；用 Probe 交互量测，或读 `.out` 打印表。